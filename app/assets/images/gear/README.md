# Gear page images

Drop any images used by `/gear` in this folder.

In `config/gear.yml`, set:

- `image: gear/<filename>`
- `image_alt: <short descriptive alt text>`

Recommended:

- JPEG or WebP
- ~1200px wide max
- Keep backgrounds clean; avoid badge-like graphics
