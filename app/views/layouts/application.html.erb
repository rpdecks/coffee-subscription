<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Acer Coffee" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/site.webmanifest">
    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="icon" href="/favicon-96x96.png" type="image/png" sizes="96x96">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
      .logo-only,
      .logo-with-text {
        display: none;
      }

      @media (max-width: 499px) {
        .logo-only {
          display: block;
        }
      }

      @media (min-width: 500px) {
        .logo-with-text {
          display: block;
        }
      }
    </style>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white text-gray-900 font-sans overflow-x-clip">
    <nav id="site-header" class="site-header w-screen bg-transparent shadow-none py-4 transition-all duration-200 ease-out">
      <div class="mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center gap-3">
          <div class="flex items-center min-w-0 overflow-hidden text-espresso">
            <%= link_to root_path, class: "flex items-center min-w-0 hover:opacity-90 transition -mt-px" do %>
              <span class="logo-only h-20 w-20 text-espresso">
                <%= render "shared/logo_icon" %>
              </span>
              <span class="logo-with-text">
                <%= render "shared/logo_full", class: "h-14" %>
              </span>
            <% end %>
          </div>

          <!-- Desktop: Centered main nav -->
          <div class="hidden md:flex items-center space-x-8 lg:space-x-10 flex-1 justify-center">
            <%= link_to "Shop", shop_path, nav_link_html_options(shop_path, variant: :desktop) %>
            <%= link_to "Products", products_path, nav_link_html_options(products_path, variant: :desktop) %>
            <%= link_to "About", about_path, nav_link_html_options(about_path, variant: :desktop) %>
            <%= link_to "FAQ", faq_path, nav_link_html_options(faq_path, variant: :desktop) %>
            <%= link_to "Contact", contact_path, nav_link_html_options(contact_path, variant: :desktop) %>
          </div>

          <!-- Desktop: Account icon dropdown -->
          <div class="hidden md:block relative">
            <button id="account-menu-button" class="flex items-center justify-center w-10 h-10 rounded-full hover:bg-gray-100 transition-colors text-gray-700 overflow-hidden bg-gray-200">
              <% if user_signed_in? && current_user.avatar.attached? %>
                <%= image_tag current_user.avatar.variant(resize_to_fill: [50, 50]), alt: current_user.full_name, class: "w-full h-full object-cover" %>
              <% else %>
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
              <% end %>
            </button>

            <!-- Account dropdown -->
            <div id="account-menu" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 py-1 z-50">
              <% if user_signed_in? %>
                <%= link_to "Dashboard", dashboard_root_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" %>
                <%= link_to "Edit Profile", edit_dashboard_profile_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" %>
                <% if current_user.admin? %>
                  <%= link_to "Admin", admin_root_path, class: "block px-4 py-2 text-red-600 hover:bg-gray-100 transition-colors font-medium" %>
                <% end %>
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" %>
              <% else %>
                <%= link_to "Sign In", new_user_session_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" %>
                <%= link_to "Sign Up", new_user_registration_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors font-medium" %>
              <% end %>
            </div>
          </div>

          <!-- Mobile menu button -->
          <button id="mobile-menu-button" class="md:hidden flex-shrink-0 p-2 text-gray-700 hover:text-gray-900" aria-controls="mobile-menu" aria-expanded="false">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile menu (full-screen overlay) -->
      <div id="mobile-menu" class="hidden md:hidden fixed inset-0 z-50" role="dialog" aria-modal="true" aria-label="Mobile navigation">
        <div id="mobile-menu-backdrop" class="absolute inset-0 bg-black/40 backdrop-blur-sm opacity-0 transition-opacity duration-200 ease-out" aria-hidden="true"></div>

        <div class="relative h-full w-full overflow-y-auto overscroll-contain flex items-start justify-center p-6">
          <div id="mobile-menu-panel" class="w-full max-w-md max-h-[calc(100vh-3rem)] overflow-y-auto rounded-card bg-white text-espresso opacity-0 translate-y-2 scale-95 transition-all duration-200 ease-out shadow-[0_35px_60px_rgba(0,0,0,0.25)]">
            <div class="px-7 py-6">
              <div class="flex items-center justify-end">
                <button id="mobile-menu-close-button" type="button" class="text-sm font-medium text-gray-600 hover:text-gray-900 transition" aria-label="Close menu">
                  Close
                </button>
              </div>
            </div>

            <div class="px-7 pb-10">
              <nav class="text-center font-serif tracking-wide">
                <% if user_signed_in? %>
                  <p class="text-sm uppercase tracking-[0.4em] text-gray-500 mb-6">Hi, <%= current_user.first_name.presence || current_user.email.to_s.split('@').first.capitalize.presence || 'Member' %></p>
                <% end %>
                <div class="space-y-5">
                  <%= link_to "Shop", shop_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                  <%= link_to "Products", products_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                  <%= link_to "About", about_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                  <%= link_to "FAQ", faq_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                  <%= link_to "Contact", contact_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                </div>

                <hr class="my-8 border-t border-gray-200">

                <div class="space-y-5">
                  <% if user_signed_in? %>
                    <%= link_to "Dashboard", dashboard_root_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                    <%= link_to "Edit Profile", edit_dashboard_profile_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                    <% if current_user.admin? %>
                      <%= link_to "Admin", admin_root_path, class: "block text-2xl py-3 rounded-brand text-deshojo hover:bg-gray-50 hover:opacity-90 transition" %>
                    <% end %>
                    <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "w-full text-center text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                  <% else %>
                    <%= link_to "Sign In", new_user_session_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                    <%= link_to "Sign Up", new_user_registration_path, class: "block text-2xl py-3 rounded-brand hover:bg-gray-50 hover:text-deshojo transition" %>
                  <% end %>
                </div>
              </nav>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <script>
      document.addEventListener('turbo:load', () => {
        // Header scroll behavior
        const siteHeader = document.getElementById('site-header');
        if (siteHeader) {
          const updateHeader = () => {
            const scrolled = window.scrollY > 20;

            siteHeader.classList.toggle('bg-white', scrolled);
            siteHeader.classList.toggle('shadow-md', scrolled);
            siteHeader.classList.toggle('py-3', scrolled);

            siteHeader.classList.toggle('bg-transparent', !scrolled);
            siteHeader.classList.toggle('shadow-none', !scrolled);
            siteHeader.classList.toggle('py-5', !scrolled);
          };

          let ticking = false;
          window.addEventListener('scroll', () => {
            if (ticking) return;
            ticking = true;
            requestAnimationFrame(() => {
              updateHeader();
              ticking = false;
            });
          }, { passive: true });

          updateHeader();
        }

        // Home page: focus primary hero CTA on load
        const heroPrimaryCta = document.getElementById('home-hero-primary-cta');
        if (heroPrimaryCta) {
          const active = document.activeElement;
          const noMeaningfulFocus = !active || active === document.body;

          if (noMeaningfulFocus) {
            // Defer to ensure layout is settled after Turbo render
            requestAnimationFrame(() => {
              try {
                heroPrimaryCta.focus({ preventScroll: true });
              } catch (e) {
                heroPrimaryCta.focus();
              }
            });
          }
        }

        // Mobile menu toggle
        const mobileButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');
        const mobileMenuBackdrop = document.getElementById('mobile-menu-backdrop');
        const mobileMenuPanel = document.getElementById('mobile-menu-panel');
        const mobileMenuCloseButton = document.getElementById('mobile-menu-close-button');

        if (mobileButton && mobileMenu && mobileMenuBackdrop && mobileMenuPanel && mobileMenuCloseButton) {
          const newMobileButton = mobileButton.cloneNode(true);
          mobileButton.parentNode.replaceChild(newMobileButton, mobileButton);

          const onMobileMenuKeyDown = (e) => {
            if (e.key === 'Escape') closeMobileMenu();
          };

          const openMobileMenu = () => {
            mobileMenu.classList.remove('hidden');
            newMobileButton.setAttribute('aria-expanded', 'true');
            document.body.classList.add('overflow-hidden');
            document.addEventListener('keydown', onMobileMenuKeyDown);

            requestAnimationFrame(() => {
              mobileMenuBackdrop.classList.remove('opacity-0');
              mobileMenuBackdrop.classList.add('opacity-100');

              mobileMenuPanel.classList.remove('opacity-0', 'translate-y-2', 'scale-95');
              mobileMenuPanel.classList.add('opacity-100', 'translate-y-0', 'scale-100');
            });
          };

          const closeMobileMenu = () => {
            newMobileButton.setAttribute('aria-expanded', 'false');
            document.body.classList.remove('overflow-hidden');
            document.removeEventListener('keydown', onMobileMenuKeyDown);

            mobileMenuBackdrop.classList.remove('opacity-100');
            mobileMenuBackdrop.classList.add('opacity-0');

            mobileMenuPanel.classList.remove('opacity-100', 'translate-y-0', 'scale-100');
            mobileMenuPanel.classList.add('opacity-0', 'translate-y-2', 'scale-95');

            window.setTimeout(() => {
              mobileMenu.classList.add('hidden');
            }, 200);
          };

          newMobileButton.addEventListener('click', () => {
            const isOpen = !mobileMenu.classList.contains('hidden');
            if (isOpen) {
              closeMobileMenu();
            } else {
              openMobileMenu();
            }
          });

          mobileMenuBackdrop.addEventListener('click', () => {
            closeMobileMenu();
          });

          mobileMenuCloseButton.addEventListener('click', () => {
            closeMobileMenu();
          });

          mobileMenu.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', () => {
              closeMobileMenu();
            });
          });
        }

        // Desktop account menu toggle
        const accountButton = document.getElementById('account-menu-button');
        const accountMenu = document.getElementById('account-menu');

        if (accountButton && accountMenu) {
          const newAccountButton = accountButton.cloneNode(true);
          accountButton.parentNode.replaceChild(newAccountButton, accountButton);

          newAccountButton.addEventListener('click', (e) => {
            e.stopPropagation();
            accountMenu.classList.toggle('hidden');
          });

          // Close when clicking outside
          document.addEventListener('click', (e) => {
            if (!accountMenu.contains(e.target) && !newAccountButton.contains(e.target)) {
              accountMenu.classList.add('hidden');
            }
          });
        }
      });
    </script>

    <% unless content_for?(:skip_container) %>
      <% if notice.present? %>
        <div class="bg-green-50 border border-green-400 px-4 py-3 rounded-lg container mx-auto my-4" role="alert">
          <p class="font-semibold text-gray-900"><%= notice %></p>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="bg-red-50 border border-red-400 px-4 py-3 rounded-lg container mx-auto my-4" role="alert">
          <p class="font-semibold text-gray-900"><%= alert %></p>
        </div>
      <% end %>
    <% end %>

    <main class="<%= 'container mx-auto px-4 pt-0 pb-8' unless content_for?(:skip_container) %>">
      <%= yield %>
    </main>

    <footer class="bg-gray-950 border-t border-gray-800 text-white mt-16">
      <div class="container mx-auto px-4 py-10 sm:py-12">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-10 text-sm">
          <div class="space-y-3">
            <div class="flex items-center gap-3">
              <span class="w-9 h-9 bg-slate-900 rounded-full flex items-center justify-center border border-gray-700">
                <%= render "shared/logo_icon", class: "text-white" %>
              </span>
              <h3 class="font-serif text-xl font-semibold tracking-tight">Acer Coffee</h3>
            </div>
            <p class="text-gray-300 leading-relaxed">Small-batch roasters crafting East Coast-ready coffees with clarity, balance, and confidence.</p>
            <p class="text-gray-500 text-xs uppercase tracking-[0.3em]">Seed to cup, roasted to taste.</p>
          </div>

          <div>
            <h3 class="font-serif text-xl font-semibold mb-4">Quick Links</h3>
            <ul class="space-y-2 text-sm text-gray-300">
              <li><%= link_to "Shop", shop_path, class: "hover:text-white transition" %></li>
              <li><%= link_to "About", about_path, class: "hover:text-white transition" %></li>
              <li><%= link_to "FAQ", faq_path, class: "hover:text-white transition" %></li>
              <li><%= link_to "Contact", contact_path, class: "hover:text-white transition" %></li>
            </ul>
          </div>

          <div class="space-y-3">
            <h3 class="font-serif text-xl font-semibold">Contact</h3>
            <p class="text-gray-300 leading-relaxed">Prefer to talk to someone? We’re here for questions about roasting, orders, or tasting notes.</p>
            <p class="text-gray-200"> <span class="text-xs uppercase tracking-[0.3em] text-gray-500">Email</span><br><%= mail_to "hello@acercoffee.com", "hello@acercoffee.com", class: "font-medium text-white hover:text-deshojo" %></p>
          </div>
        </div>

        <div class="border-t border-gray-900 mt-10 pt-8 text-center text-gray-500 text-xs sm:text-sm">
          <p>© 2026 Acer Coffee. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
