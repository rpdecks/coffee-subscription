<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Acer Coffee" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white text-gray-900 font-sans overflow-x-clip">
    <nav class="bg-white w-screen">
      <div class="mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16 gap-3">
          <div class="flex items-center min-w-0 overflow-hidden">
            <%= link_to root_path, class: "flex items-center gap-3 min-w-0 hover:opacity-90 transition text-gray-900" do %>
              <%# Coffee bean icon by Alyssa from the Noun Project %>
              <svg class="h-8 w-8 flex-shrink-0" viewBox="0 0 90 90" fill="currentColor">
                <path d="M16.112,27.895c0,0-19.206,34.839,5.125,52.456c2.723,1.711,4.586-0.553,4.789-3.621c0.201-3.067,3.895-21.867,21.027-38.446C64.186,21.702,65.23,10.451,60.84,5.655C56.344,0.742,32.885-2.691,16.112,27.895z"/>
                <path d="M73.886,62.105c0,0,19.207-34.838-5.124-52.454c-2.725-1.713-4.588,0.551-4.789,3.621c-0.201,3.066-3.893,21.866-21.028,38.445C25.813,68.297,24.771,79.551,29.158,84.347C33.655,89.258,57.112,92.692,73.886,62.105z"/>
              </svg>
              <span class="text-xl font-serif block truncate">Acer Coffee</span>
            <% end %>
          </div>

          <!-- Desktop: Centered main nav -->
          <div class="hidden md:flex items-center space-x-8 flex-1 justify-center">
            <%= link_to "Shop", shop_path, class: "text-gray-700 hover:text-gray-900 transition-colors" %>
            <%= link_to "Products", products_path, class: "text-gray-700 hover:text-gray-900 transition-colors" %>
            <%= link_to "About", about_path, class: "text-gray-700 hover:text-gray-900 transition-colors" %>
            <%= link_to "FAQ", faq_path, class: "text-gray-700 hover:text-gray-900 transition-colors" %>
            <%= link_to "Contact", contact_path, class: "text-gray-700 hover:text-gray-900 transition-colors" %>
          </div>

          <!-- Desktop: Account icon dropdown -->
          <div class="hidden md:block relative">
            <button id="account-menu-button" class="flex items-center justify-center w-10 h-10 rounded-full hover:bg-gray-100 transition-colors text-gray-700">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
              </svg>
            </button>

            <!-- Account dropdown -->
            <div id="account-menu" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 py-1 z-50">
              <% if user_signed_in? %>
                <%= link_to "Dashboard", dashboard_root_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" %>
                <% if current_user.admin? %>
                  <%= link_to "Admin", admin_root_path, class: "block px-4 py-2 text-red-600 hover:bg-gray-100 transition-colors font-medium" %>
                <% end %>
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" %>
              <% else %>
                <%= link_to "Sign In", new_user_session_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" %>
                <%= link_to "Sign Up", new_user_registration_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors font-medium" %>
              <% end %>
            </div>
          </div>

          <!-- Mobile menu button -->
          <button id="mobile-menu-button" class="md:hidden flex-shrink-0 p-2 text-gray-700 hover:text-gray-900">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile menu -->
      <div id="mobile-menu" class="hidden md:hidden bg-white">
        <div class="mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8 py-3 space-y-3">
          <!-- Site navigation -->
          <%= link_to "Shop", shop_path, class: "block py-2 nav-link" %>
          <%= link_to "Products", products_path, class: "block py-2 nav-link" %>
          <%= link_to "About", about_path, class: "block py-2 nav-link" %>
          <%= link_to "FAQ", faq_path, class: "block py-2 nav-link" %>
          <%= link_to "Contact", contact_path, class: "block py-2 nav-link" %>

          <!-- Divider -->
          <hr class="border-t border-gray-200">

          <!-- Account section -->
          <% if user_signed_in? %>
            <%= link_to "Dashboard", dashboard_root_path, class: "block py-2 nav-link" %>
            <% if current_user.admin? %>
              <%= link_to "Admin", admin_root_path, class: "block py-2 text-red-600 hover:text-red-700 font-medium" %>
            <% end %>
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-left py-2 nav-link w-full" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "block py-2 nav-link" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "block py-2 nav-link font-medium" %>
          <% end %>
        </div>
      </div>
    </nav>

    <script>
      document.addEventListener('turbo:load', () => {
        // Mobile menu toggle
        const mobileButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');

        if (mobileButton && mobileMenu) {
          const newMobileButton = mobileButton.cloneNode(true);
          mobileButton.parentNode.replaceChild(newMobileButton, mobileButton);

          newMobileButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
          });

          mobileMenu.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', () => {
              mobileMenu.classList.add('hidden');
            });
          });
        }

        // Desktop account menu toggle
        const accountButton = document.getElementById('account-menu-button');
        const accountMenu = document.getElementById('account-menu');

        if (accountButton && accountMenu) {
          const newAccountButton = accountButton.cloneNode(true);
          accountButton.parentNode.replaceChild(newAccountButton, accountButton);

          newAccountButton.addEventListener('click', (e) => {
            e.stopPropagation();
            accountMenu.classList.toggle('hidden');
          });

          // Close when clicking outside
          document.addEventListener('click', (e) => {
            if (!accountMenu.contains(e.target) && !newAccountButton.contains(e.target)) {
              accountMenu.classList.add('hidden');
            }
          });
        }
      });
    </script>

    <% unless content_for?(:skip_container) %>
      <% if notice.present? %>
        <div class="bg-green-50 border border-green-400 px-4 py-3 rounded-lg container mx-auto my-4" role="alert">
          <p class="font-semibold text-gray-900"><%= notice %></p>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="bg-red-50 border border-red-400 px-4 py-3 rounded-lg container mx-auto my-4" role="alert">
          <p class="font-semibold text-gray-900"><%= alert %></p>
        </div>
      <% end %>
    <% end %>

    <main class="<%= 'container mx-auto px-4 pt-0 pb-8' unless content_for?(:skip_container) %>">
      <%= yield %>
    </main>

    <footer class="bg-gray-900 border-t border-gray-800 text-white mt-16">
      <div class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div>
            <h3 class="font-serif text-lg font-semibold mb-4">Acer Coffee</h3>
            <p class="text-gray-400 text-sm">Freshly roasted coffee, delivered to your door.</p>
          </div>
          <div>
            <h3 class="font-serif text-lg font-semibold mb-4">Quick Links</h3>
            <ul class="space-y-2 text-sm">
              <li><%= link_to "Products", products_path, class: "text-gray-400 hover:text-white" %></li>
              <li><%= link_to "About", about_path, class: "text-gray-400 hover:text-white" %></li>
              <li><%= link_to "FAQ", faq_path, class: "text-gray-400 hover:text-white" %></li>
              <li><%= link_to "Contact", contact_path, class: "text-gray-400 hover:text-white" %></li>
            </ul>
          </div>
          <div>
            <h3 class="font-serif text-lg font-semibold mb-4">Contact</h3>
            <p class="text-gray-400 text-sm">Questions? <%= link_to "Get in touch", contact_path, class: "text-white hover:text-gray-300 underline" %></p>
          </div>
        </div>
        <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-500 text-sm">
          <p>&copy; <%= Time.current.year %> Acer Coffee. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
